language: python
python:
    - 3.3
    - 3.4
    - 3.5
    - 3.6
install:
    - pip install -r requirements-dev.txt
    - pip install coveralls
script:
    - python -m flake8
    - py.test tests -v --cov=pygsmmodule
after_success:
    - coveralls --verbose
